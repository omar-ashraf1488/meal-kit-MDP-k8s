apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# Debezium connectors organized by app
# Add new app directories here as they are created

resources:
  - chefbot-sim/
  # - engagement-sim/   # Future: PostgreSQL connector
  # - marketing-sim/    # Future: MongoDB connector
